[Unit]
Requires=attest.service
After=attest.service

[Service]
# Only allow kubelet to start if the last attestation status is "passed"
ExecStartPre=/usr/bin/jq -e '.status == "passed"' /var/lib/attest/status.json
